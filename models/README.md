# Model Binaries and Configs

Store compiled models here.
- `web_model/`: TensorFlow.js model files (e.g. `model.json` and weight `.bin` shards).
- `onnx/`: ONNX models (`.onnx`) for high-performance edge execution.
